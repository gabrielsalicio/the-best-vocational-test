<!DOCTYPE html>
<html>
<head>
	<meta charset="utf-8">
	<title></title>
	<link rel="stylesheet" type="text/css" href='/stylesheet.css'/>
</head>
<body >
	<div>
		<form action='/responses' method='POST'>
			<ul>
				<% @questions.map do |question| %> 
				<li id="ques"> 
					<%= question.description %><br> 
					<ul>
						<% question.choices.map do |choice| %> 
						<input required type='radio' name='<%= question.id%>' value='<%= choice.id %>' > <%= choice.text %><br>
						<% end %>
						<input type='hidden' name='question_id[]' value='<%= question.id %>'>
					</ul>
					<% end %>
				</li>
			</ul>
			<input type='hidden' name='survey_id' value='<%= @user.id %>'>
			<p id = "alert">
				<b> Debes seleecionar al menos una opcion de todas las preguntas.</b>
			</p>
			<input type = 'submit' value = 'Enviar respuestas'>
		</form> 
		</div>
</body>
</html>

